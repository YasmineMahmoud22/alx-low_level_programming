#include "main.h"

/**
* _isupper - checks uppercase char.
* @c:  checked char.
* Return: 1 if the number is a digit.
*/
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
return (1);

else
return (0);
}
